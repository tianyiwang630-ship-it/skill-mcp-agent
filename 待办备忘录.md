# 待办备忘录

## agent-alpha bash 工具

1. bash 工具执行终端选择需要支持启动时配置，允许在 PowerShell、Git Bash、WSL、cmd 等 shell 之间切换。
2. bash 的安全约束还需要继续参考 hermes-agent 的规则，完善命令分类、权限确认和危险命令拦截策略。
3. npm 和 Go 依赖安装边界暂时维持现状：当前只做包安装权限提醒，尚未强制把 npm cache/prefix、Go 的 GOBIN/GOMODCACHE/GOCACHE 压进 agent-alpha 内部；后续如要支持 skill 自动依赖安装，需要单独收束这一项。
